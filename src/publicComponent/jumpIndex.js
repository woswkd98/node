
import React , {useState} from 'react';

 const jumpIndex = ({length, limit}) => {
    
   // const [indexList, setIndexList] = useState([]); 

    const setList = () => {
        let list = [];
        /*
        for (let index = 0; index < Math.cell(length / limit); index++) {
            list.push(<th onClick = {
                // 상태 넘겨줘야함
            }>index</th>)
        }
        */
        return list;
    }


    return (
        <div>
            
        </div>
    );
}

export default jumpIndex;